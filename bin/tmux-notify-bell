#!/bin/bash

TITLE="$1"
ZWS=$'\xe2\x80\x8b'

case "$TITLE" in
  *"$ZWS"*)
    CLEAN="${TITLE//$ZWS/}"
    LABEL="${CLEAN#󰌗 }"
    MSG="Claude is waiting for input"
    osascript -e "display notification \"${MSG//\"/\\\"}\" with title \"Claude Code\" subtitle \"${LABEL//\"/\\\"}\""
    ;;
esac